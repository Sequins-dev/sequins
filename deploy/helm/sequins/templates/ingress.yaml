{{- if and .Values.queryApi.enabled .Values.queryApi.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "sequins.fullname" . }}
  labels:
    {{- include "sequins.labels" . | nindent 4 }}
  {{- with .Values.queryApi.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.queryApi.ingress.className }}
  ingressClassName: {{ .Values.queryApi.ingress.className }}
  {{- end }}
  {{- if .Values.queryApi.ingress.tls }}
  tls:
    {{- range .Values.queryApi.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.queryApi.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "sequins.fullname" $ }}-query-api
                port:
                  number: {{ $.Values.queryApi.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
